{% extends "base.html.twig" %}
{% block header %}
{% endblock %}
{% block body %}
    <link rel="stylesheet" href="{{ asset('css/stylesheet1.css') }}">
    <div class="container1">
        <div class="container" style="background-color:rgba(255,255,255,0.6);">
            <div style="text-align:center;">
                <div class="col-md-4 col-md-offset-4">
                    <h2 style="color: blue">Welcome to Our Library</h2>
                    <h3>Please Register</h3>
                    <br>

                    {{ form_start(form) }}
                    {{ form_widget(form) }}
                    <button type="submit" class="btn btn-primary" formnovalidate>Register</button>
                    {{ form_end(form) }}
                    <br>
                    <a href="{{ path('login') }}" style="color: white">
                        <button type="submit" class="btn btn-primary">Log In</button>
                    </a>
                    <p></p>

                </div>
            </div>
        </div>
    </div>
{% endblock %}